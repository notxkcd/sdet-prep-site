---
title: "Java Programming Study Guide - Aiite Q Dump - Set_2"
date: 2026-01-30
draft: false
weight: 6
categories: ["Java Programs"]
---


## Beginner (Basics)
1. [Reverse a string?](#1-reverse-a-string)
2. [Reverse an integer number?](#2-reverse-an-integer-number)
3. [Count vowels in a string (e.g., "welcome")?](#3-count-vowels-in-a-string-eg-welcome)
4. [Print vowels from a string?](#4-print-vowels-from-a-string)
5. [Find the maximum and minimum numbers in an array?](#5-find-the-maximum-and-minimum-numbers-in-an-array)
6. [Swap two numbers without using a third variable?](#6-swap-two-numbers-without-using-a-third-variable)
7. [Find the count of letters in a string (Input: " Hello ")?](#7-find-the-count-of-letters-in-a-string-input-hello-)
8. [Replace '-' with '/' in a date string (Input: "03-06-1995")?](#8-replace---with-in-a-date-string-input-03-06-1995)
9. [How to get system date and time?](#9-how-to-get-system-date-and-time)
10. [Convert a string to an integer (Input: "123456")?](#10-convert-a-string-to-an-integer-input-123456)
11. [Get all variables from an array using a loop?](#11-get-all-variables-from-an-array-using-a-loop)

## Intermediate (Intermediate Logic)
1. [Reverse a string word by word?](#1-reverse-a-string-word-by-word)
2. [Count duplicate characters in a string?](#2-count-duplicate-characters-in-a-string)
3. [Find the frequency of characters in a string?](#3-find-the-frequency-of-characters-in-a-string)
4. [Find duplicate words in a string (e.g., "Hexaware")?](#4-find-duplicate-words-in-a-string-eg-hexaware)
5. [Reverse two numbers without using a temporary variable (Input: `x = 10`, `y = 20`)?](#5-reverse-two-numbers-without-using-a-temporary-variable-input-x-10-y-20)
6. [Reverse two strings without using a temporary variable (Input: `a = "India"`, `b = "uk"`)?](#6-reverse-two-strings-without-using-a-temporary-variable-input-a-india-b-uk)
7. [Find the second highest number in an array?](#7-find-the-second-highest-number-in-an-array)
8. [Find duplicate elements in an array?](#8-find-duplicate-elements-in-an-array)
9. [Sort an array?](#9-sort-an-array)
10. [Convert an array to a list?](#10-convert-an-array-to-a-list)
11. [Retrieve key-value pairs from a `HashMap` (Input: `X = "A", Y = "B"`)?](#11-retrieve-key-value-pairs-from-a-hashmap-input-x-a-y-b)
12. [Write a POJO class for Username and Password?](#12-write-a-pojo-class-for-username-and-password)

## Advanced (Advanced Algorithms & Logic)
1. [Reverse a string word by word but keep word position?](#1-reverse-a-string-word-by-word-but-keep-word-position)
2. [Find repeated characters in the word "ASSASINATION"?](#2-find-repeated-characters-in-the-word-assasination)
3. [Remove duplicates from a string without using collection concepts?](#3-remove-duplicates-from-a-string-without-using-collection-concepts)
4. [Anagram check: Determine if two string patterns are the same (e.g., "CAT" and "ACT")?](#4-anagram-check-determine-if-two-string-patterns-are-the-same-eg-cat-and-act)
5. [Count characters, numbers, and special characters in a mixed string?](#5-count-characters-numbers-and-special-characters-in-a-mixed-string)
6. [Find the 3rd maximum element in an array?](#6-find-the-3rd-maximum-element-in-an-array)
7. [Sum of two integer arrays?](#7-sum-of-two-integer-arrays)
8. [Find common duplicates between two arrays?](#8-find-common-duplicates-between-two-arrays)
9. [Logic to connect to a database?](#9-logic-to-connect-to-a-database)

---

## Questions with Answers

## Beginner (Basics)

### 1. Reverse a string?
**Solution**:
```java
String rev = new StringBuilder(s).reverse().toString();
```

### 2. Reverse an integer number?
**Solution**:
```java
int rev = 0; while(n != 0) { rev = rev * 10 + n % 10; n /= 10; }
```

### 3. Count vowels in a string?
**Solution**:
```java
long count = s.toLowerCase().chars().filter(c -> "aeiou".indexOf(c) != -1).count();
```

### 4. Print vowels from a string?
**Solution**:
```java
for(char c : s.toCharArray()) if("aeiouAEIOU".indexOf(c) != -1) System.out.println(c);
```

### 5. Find the maximum and minimum numbers in an array?
**Solution**:
```java
int max = arr[0], min = arr[0];
for(int x : arr) { if(x > max) max = x; if(x < min) min = x; }
```

### 6. Swap two numbers without using a third variable?
**Solution**:
```java
a = a + b; b = a - b; a = a - b;
```

### 7. Find the count of letters in a string?
**Solution**:
```java
int count = s.replaceAll("[^a-zA-Z]", "").length();
```

### 8. Replace '-' with '/' in a date string?
**Solution**:
```java
String res = s.replace('-', '/');
```

### 9. How to get system date and time?
**Solution**:
```java
System.out.println(java.time.LocalDateTime.now());
```

### 10. Convert a string to an integer?
**Solution**:
```java
int n = Integer.parseInt(s);
```

### 11. Get all variables from an array using a loop?
**Solution**:
```java
for(int x : arr) System.out.println(x);
```

## Intermediate (Intermediate Logic)

### 1. Reverse a string word by word?
**Solution**:
```java
String[] w = s.split(" ");
String res = "";
for(int i=w.length-1; i>=0; i--) res += w[i] + " ";
```

### 2. Count duplicate characters in a string?
**Solution**:
```java
Map<Character, Integer> map = new HashMap<>();
for(char c : s.toCharArray()) map.put(c, map.getOrDefault(c, 0) + 1);
map.forEach((k, v) -> { if(v > 1) System.out.println(k); });
```

### 3. Find the frequency of characters in a string?
**Solution**:
```java
Map<Character, Integer> map = new HashMap<>();
for(char c : s.toCharArray()) map.put(c, map.getOrDefault(c, 0) + 1);
```

### 4. Find duplicate words in a string?
**Solution**:
```java
String[] words = s.split(" ");
Set<String> set = new HashSet<>();
for(String w : words) if(!set.add(w)) System.out.println(w);
```

### 5. Reverse two numbers without using a temporary variable?
**Solution**:
```java
x = x + y; y = x - y; x = x - y;
```

### 6. Reverse two strings without using a temporary variable?
**Solution**:
```java
a = a + b; b = a.substring(0, a.length()-b.length()); a = a.substring(b.length());
```

### 7. Find the second highest number in an array?
**Solution**:
```java
Arrays.sort(arr);
int res = arr[arr.length - 2];
```

### 8. Find duplicate elements in an array?
**Solution**:
```java
Set<Integer> set = new HashSet<>();
for(int x : arr) if(!set.add(x)) System.out.println(x);
```

### 9. Sort an array?
**Solution**:
```java
Arrays.sort(arr);
```

### 10. Convert an array to a list?
**Solution**:
```java
List<Integer> list = Arrays.asList(arr);
```

### 11. Retrieve key-value pairs from a `HashMap`?
**Solution**:
```java
map.forEach((k, v) -> System.out.println(k + "=" + v));
```

### 12. Write a POJO class for Username and Password?
**Solution**:
```java
class User {
    private String u, p;
    public String getU() { return u; } public void setU(String u) { this.u = u; }
    public String getP() { return p; } public void setP(String p) { this.p = p; }
}
```

## Advanced (Advanced Algorithms & Logic)

### 1. Reverse a string word by word but keep word position?
**Solution**:
```java
String res = Arrays.stream(s.split(" ")).map(w -> new StringBuilder(w).reverse().toString()).collect(Collectors.joining(" "));
```

### 2. Find repeated characters in the word "ASSASINATION"?
**Solution**:
```java
// Use HashMap logic as shown in intermediate section
```

### 3. Remove duplicates from a string without using collection concepts?
**Solution**:
```java
String res = "";
for(int i=0; i<s.length(); i++) if(!res.contains(""+s.charAt(i))) res += s.charAt(i);
```

### 4. Anagram check?
**Solution**:
```java
char[] c1 = s1.toCharArray(); char[] c2 = s2.toCharArray();
Arrays.sort(c1); Arrays.sort(c2);
boolean res = Arrays.equals(c1, c2);
```

### 5. Count characters, numbers, and special characters in a mixed string?
**Solution**:
```java
int l=0, d=0, sp=0;
for(char c : s.toCharArray()) {
    if(Character.isLetter(c)) l++; else if(Character.isDigit(c)) d++; else sp++;
}
```

### 6. Find the 3rd maximum element in an array?
**Solution**:
```java
Arrays.sort(arr); int res = arr[arr.length - 3];
```

### 7. Sum of two integer arrays?
**Solution**:
```java
int[] sum = new int[a.length];
for(int i=0; i<a.length; i++) sum[i] = a[i] + b[i];
```

### 8. Find common duplicates between two arrays?
**Solution**:
```java
Set<Integer> s1 = new HashSet<>(Arrays.asList(a));
for(int x : b) if(s1.contains(x)) System.out.println(x);
```

### 9. Logic to connect to a database?
**Solution**:
```java
Connection con = DriverManager.getConnection(url, user, password);
```