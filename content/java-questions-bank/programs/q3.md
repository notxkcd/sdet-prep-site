---
title: "Java Programming Study Guide - AiiTE Q Dump-Set_7"
date: 2026-01-30
draft: false
weight: 3
categories: ["Java Programs"]
---


## Beginner (Basic Strings & Numbers)
1. [Reverse a string?](#1-reverse-a-string)
2. [Find the number of alphabets in a sentence?](#2-find-the-number-of-alphabets-in-a-sentence)
3. [Separate numbers only from a string (Input: "09/24/2015")?](#3-separate-numbers-only-from-a-string-input-09242015)
4. [Write a Factorial program?](#4-write-a-factorial-program)
5. [Swap two variables without using a 3rd variable?](#5-swap-two-variables-without-using-a-3rd-variable)
6. [Star pattern program (e.g., Pyramid)?](#6-star-pattern-program-eg-pyramid)
7. [Write a program for an `ArrayList` to add elements and remove the first one?](#7-write-a-program-for-an-arraylist-to-add-elements-and-remove-the-first-one)
8. [Write a program that generates an `ArithmeticException` (Divide by zero) and handles it?](#8-write-a-program-that-generates-an-arithmeticexception-divide-by-zero-and-handles-it)

## Intermediate (Intermediate Logic & Collections)
1. [Reverse each word in a string (Input: "Good Girl")?](#1-reverse-each-word-in-a-string-input-good-girl)
2. [Remove duplicate words or characters from a string?](#2-remove-duplicate-words-or-characters-from-a-string)
3. [Check if a string is a Palindrome?](#3-check-if-a-string-is-a-palindrome)
4. [Find the second largest number in an array?](#4-find-the-second-largest-number-in-an-array)
5. [Print descending order of an array?](#5-print-descending-order-of-an-array)
6. [Sort an array in ascending order?](#6-sort-an-array-in-ascending-order)
7. [Print non-repeating numbers in an array?](#7-print-non-repeating-numbers-in-an-array)
8. [Write a program to loop through an `ArrayList` using four different methods?](#8-write-a-program-to-loop-through-an-arraylist-using-four-different-methods)
9. [Demonstrate the use of the `finally` keyword in a program?](#9-demonstrate-the-use-of-the-finally-keyword-in-a-program)

## Advanced (Algorithms & Complex Logic)
1. [Count the occurrence of each character in a string?](#1-count-the-occurrence-of-each-character-in-a-string)
2. [Find the occurrence of every character in "Welcome to Wipro"?](#2-find-the-occurrence-of-every-character-in-welcome-to-wipro)
3. [Print all possible combinations of a string (Input: "abc")?](#3-print-all-possible-combinations-of-a-string-input-abc)
4. [Demonstrate the use of the `throws` keyword in a program?](#4-demonstrate-the-use-of-the-throws-keyword-in-a-program)

---

## Questions with Answers

## Beginner (Basic Strings & Numbers)

### 1. Reverse a string?
**Solution**:
```java
String s = "Java";
String rev = new StringBuilder(s).reverse().toString();
```

### 2. Find the number of alphabets in a sentence?
**Solution**:
```java
int count = s.replaceAll("[^a-zA-Z]", "").length();
```

### 3. Separate numbers only from a string (Input: "09/24/2015")?
**Solution**:
```java
String s = "09/24/2015";
String res = s.replaceAll("[^0-9]", "");
```

### 4. Write a Factorial program?
**Solution**:
```java
int f=1; for(int i=1; i<=n; i++) f*=i;
```

### 5. Swap two variables without using a 3rd variable?
**Solution**:
```java
a = a + b; b = a - b; a = a - b;
```

### 6. Star pattern program (Pyramid)?
**Solution**:
```java
for (int i=0; i<n; i++) {
    for (int j=n-i; j>1; j--) System.out.print(" ");
    for (int j=0; j<=i; j++) System.out.print("* ");
    System.out.println();
}
```

### 7. Write a program for an `ArrayList` to add elements and remove the first one?
**Solution**:
```java
List<String> list = new ArrayList<>();
list.add("First"); list.add("Second");
list.remove(0);
```

### 8. Write a program that generates an `ArithmeticException` (Divide by zero) and handles it?
**Solution**:
```java
try {
    int res = 10 / 0;
} catch (ArithmeticException e) {
    System.out.println("Cannot divide by zero");
}
```

## Intermediate (Intermediate Logic & Collections)

### 1. Reverse each word in a string (Input: "Good Girl")?
**Solution**:
```java
String[] words = s.split(" ");
String res = "";
for(String w : words) res += new StringBuilder(w).reverse() + " ";
```

### 2. Remove duplicate words or characters from a string?
**Solution**:
```java
String res = Arrays.stream(s.split(" ")).distinct().collect(Collectors.joining(" "));
```

### 3. Check if a string is a Palindrome?
**Solution**:
```java
boolean isPal = s.equals(new StringBuilder(s).reverse().toString());
```

### 4. Find the second largest number in an array?
**Solution**:
```java
Arrays.sort(arr);
int res = arr[arr.length - 2];
```

### 5. Print descending order of an array?
**Solution**:
```java
Arrays.sort(arr);
for(int i=arr.length-1; i>=0; i--) System.out.println(arr[i]);
```

### 6. Sort an array in ascending order?
**Solution**:
```java
Arrays.sort(arr);
```

### 7. Print non-repeating numbers in an array?
**Solution**:
```java
Map<Integer, Integer> map = new HashMap<>();
for(int x : arr) map.put(x, map.getOrDefault(x, 0) + 1);
map.forEach((k, v) -> { if(v == 1) System.out.println(k); });
```

### 8. Write a program to loop through an `ArrayList` using four different methods?
**Solution**:
```java
// 1. For loop, 2. Enhanced For, 3. Iterator, 4. forEach()
for(int i=0; i<list.size(); i++);
for(String s : list);
Iterator<String> it = list.iterator();
list.forEach(System.out::println);
```

### 9. Demonstrate the use of the `finally` keyword in a program?
**Solution**:
```java
try { int x = 5/0; }
finally { System.out.println("Always runs"); }
```

## Advanced (Algorithms & Complex Logic)

### 1. Count the occurrence of each character in a string?
**Solution**:
```java
Map<Character, Integer> map = new HashMap<>();
for(char c : s.toCharArray()) map.put(c, map.getOrDefault(c, 0) + 1);
```

### 2. Find the occurrence of every character in "Welcome to Wipro"?
**Solution**:
```java
// Same logic as character count above
```

### 3. Print all possible combinations of a string (Input: "abc")?
**Solution**:
```java
// Recursive function to print permutations
```

### 4. Demonstrate the use of the `throws` keyword in a program?
**Solution**:
```java
void myMethod() throws IOException {
    throw new IOException("Error");
}
```