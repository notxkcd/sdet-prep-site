---
title: "Java Programming Study Guide - Java Q_Set_1_and_2"
date: 2026-01-30
draft: false
weight: 11
categories: ["Java Programs"]
---


## Intermediate
1. [Write a program to convert a `HashMap` to an `Array`?](#1-write-a-program-to-convert-a-hashmap-to-an-array)

## Advanced
1. [Write a program using a `HashMap` to find the number of occurrences of each character in a string?](#1-write-a-program-using-a-hashmap-to-find-the-number-of-occurrences-of-each-character-in-a-string)

---

## Questions with Answers

## Intermediate

### 1. Write a program to convert a `HashMap` to an `Array`?
**Solution**:
```java
HashMap<Integer, String> map = new HashMap<>();
// Convert values to array
String[] values = map.values().toArray(new String[0]);
// Convert keys to array
Integer[] keys = map.keySet().toArray(new Integer[0]);
```

## Advanced

### 1. Write a program using a `HashMap` to find the number of occurrences of each character in a string?
**Solution**:
```java
String s = "interview";
Map<Character, Integer> map = new HashMap<>();
for(char c : s.toCharArray()) {
    map.put(c, map.getOrDefault(c, 0) + 1);
}
System.out.println(map);
```
