---
title: "Java Programming Study Guide - Aiite Q Dump - Set_5"
date: 2026-01-30
draft: false
weight: 9
categories: ["Java Programs"]
---


## Beginner (Basics)
1. [Write a program to reverse a string?](#1-write-a-program-to-reverse-a-string)
2. [Write a program to find even and odd numbers in a given array?](#2-write-a-program-to-find-even-and-odd-numbers-in-a-given-array)
3. [Swap two numbers with and without using a third variable?](#3-swap-two-numbers-with-and-without-using-a-third-variable)

## Intermediate (Intermediate Logic)
1. [Write a program to count the occurrences of a specific character (e.g., 'a') in a given string?](#1-write-a-program-to-count-the-occurrences-of-a-specific-character-eg-a-in-a-given-string)
2. [Write a program to count the number of words in a given sentence?](#2-write-a-program-to-count-the-number-of-words-in-a-given-sentence)
3. [Swap two strings without using a temporary variable?](#3-swap-two-strings-without-using-a-temporary-variable)
4. [Write a program to remove duplicate elements from an array?](#4-write-a-program-to-remove-duplicate-elements-from-an-array)

---

## Questions with Answers

## Beginner (Basics)

### 1. Write a program to reverse a string?
**Solution**:
```java
String s = "Java";
String rev = new StringBuilder(s).reverse().toString();
```

### 2. Write a program to find even and odd numbers in a given array?
**Solution**:
```java
for(int x : arr) if(x % 2 == 0) System.out.println("Even: " + x); else System.out.println("Odd: " + x);
```

### 3. Swap two numbers with and without using a third variable?
**Solution**:
- **With 3rd variable**: `int temp = a; a = b; b = temp;`
- **Without 3rd variable**: `a = a + b; b = a - b; a = a - b;`

## Intermediate (Intermediate Logic)

### 1. Write a program to count the occurrences of a specific character (e.g., 'a') in a given string?
**Solution**:
```java
String s = "java language";
long count = s.chars().filter(ch -> ch == 'a').count();
```

### 2. Write a program to count the number of words in a given sentence?
**Solution**:
```java
String s = "Java is fun";
int words = s.trim().split("\\s+").length;
```

### 3. Swap two strings without using a temporary variable?
**Solution**:
```java
a = a + b;
b = a.substring(0, a.length() - b.length());
a = a.substring(b.length());
```

### 4. Write a program to remove duplicate elements from an array?
**Solution**:
```java
Set<Integer> set = new LinkedHashSet<>(Arrays.asList(arr));
// Set now contains unique elements in order
```
